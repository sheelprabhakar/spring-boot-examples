spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org:h2:Driver
    username: a
    password:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org:hibernate:dialect:H2Dialect
    hibernate:
      ddl-auto: update