spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    platform: h2

    jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org:hibernate:dialect:H2Dialect
    hibernate:
      ddl-auto: update

code4copy:
  trace: true